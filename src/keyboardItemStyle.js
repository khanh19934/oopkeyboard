import { StyleSheet,Dimensions } from 'react-native';

export default StyleSheet.create({
    container:{
        width:'30%',
        height:'100%',
        borderWidth:1.5,
        borderColor:'#dbdbdb',
        borderRadius:40,
        justifyContent:'center',
        alignItems:'center',
        marginLeft:8
    },
    textStyle:{
        fontSize:30
    }
});